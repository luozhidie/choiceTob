<view class="page">
  <!-- ========== 头部：头像+用户信息+操作图标 ========== -->
  <view class="header">
    <view class="header-left" bindtap="goSettings">
      <image class="avatar" src="{{avatarUrl||'/assets/avatar-ph.png'}}" mode="aspectFill"/>
      <view class="user-meta">
        <text class="uid">{{userId||'未登录'}}</text>
        <text class="role">{{roleText||'点击登录/注册'}}</text>
        <text class="cert-badge" wx:if="{{isCertified}}">🛡️ 已认证</text>
      </view>
    </view>
    <view class="header-icons">
      <view class="hi" bindtap="goContact"><text>🎧</text></view>
      <view class="hi" bindtap="goSettings"><text>⚙</text></view>
      <view class="hi"><text>🔔</text></view>
    </view>
  </view>

  <!-- ========== 成长等级卡（渐进解锁）========== -->
  <view class="tier-card-wrap">
    <!-- 等级卡 -->
    <view class="tier-card tier-{{curTier.key}}">
      <view class="tc-header">
        <view class="tc-left">
          <text class="tc-emoji">{{curTier.emoji}}</text>
          <view class="tc-meta">
            <text class="tc-label">当前成长等级</text>
            <text class="tc-name">{{curTier.name}}</text>
          </view>
        </view>
        <view class="tc-upgrade" bindtap="goVip">
          <text>升级特权</text>
        </view>
      </view>
      <block wx:if="{{nextTier}}">
        <view class="tc-progress-row">
          <text class="tc-progress-l">距 {{nextTier.name}} 还差</text>
          <text class="tc-progress-r">¥{{tierDiff}} · 累计 ¥{{totalSpentYuan}}</text>
        </view>
        <view class="tc-bar-track">
          <view class="tc-bar-fill" style="width:{{tierProgress}}%"></view>
        </view>
      </block>
      <view wx:else class="tc-max-tag">🎉 已达最高等级</view>
    </view>

    <!-- 等级阶梯 -->
    <view class="tier-steps">
      <block wx:for="{{tiers}}" wx:key="key">
        <view class="ts-node-wrap">
          <block wx:if="{{index < tiers.length - 1}}">
            <view class="ts-line {{index < tierIdx ? 'ts-line-active' : ''}}"></view>
          </block>
          <view class="ts-dot {{index <= tierIdx ? 'ts-dot-active' : ''}} {{index === tierIdx ? 'ts-dot-current' : ''}}">
            <text wx:if="{{index < tierIdx}}">✓</text>
            <text wx:else>{{index + 1}}</text>
          </view>
          <text class="ts-name {{index === tierIdx ? 'ts-name-cur' : ''}}">{{item.name.replace('会员','')}}</text>
        </view>
      </block>
    </view>
    <text class="tc-footnote">成长等级依据累计拿货金额自动解锁 · </text>
    <text class="tc-link" bindtap="goVip">查看权益规则</text>
  </view>

  <!-- ========== 权益解锁网格 ========== -->
  <view class="benefit-grid-wrap">
    <view class="bg-title">会员权益<text class="bg-sub">升级自动解锁</text></view>
    <view class="benefit-grid">
      <block wx:for="{{benefits}}" wx:key="key">
        <view class="bg-item {{tierIdx >= item.tier ? 'bg-on' : 'bg-off'}}">
          <view class="bg-icon-circle {{tierIdx >= item.tier ? 'bg-icon-on' : 'bg-icon-off'}}">
            <text wx:if="{{tierIdx >= item.tier}}">{{item.icon}}</text>
            <text wx:else>🔒</text>
          </view>
          <text class="bg-t {{tierIdx >= item.tier ? 'bg-t-on' : 'bg-t-off'}}">{{item.title}}</text>
          <text class="bg-sub2">{{tierIdx >= item.tier ? '已解锁' : (tiers[item.tier].name.replace('会员','')+'解锁')}}</text>
        </view>
      </block>
    </view>
  </view>

  <!-- ========== 认证店主（平行赛道）========== -->
  <view class="cert-track-wrap">
    <view class="ct-head">
      <text class="ct-title">认证店主</text>
      <text class="ct-badge" wx:if="{{isCertified}}">已认证</text>
      <text class="ct-sub" wx:else>平行赛道 · 免费</text>
    </view>
    <block wx:if="{{isCertified}}">
      <view class="ct-body-done">
        <text class="ct-check">✅</text>
        <text class="ct-msg">已通过认证 · 享全部商品批发价查看</text>
      </view>
    </block>
    <block wx:else>
      <text class="ct-desc">免费通过行业知识答题，解锁批发价查看权限（与会员等级平行不冲突）</text>
      <view class="ct-btn" bindtap="goCertify">免费认证看价 →</view>
    </block>
  </view>

  <!-- ========== 快捷数据（3格）========== -->
  <view class="stats-row">
    <view class="stat-card" bindtap="goBuyer">
      <text class="sc-icon">🏪</text>
      <text class="sc-num">{{subCount}}</text>
      <text class="sc-label">订阅档口</text>
    </view>
    <view class="stat-card" bindtap="goFavorites">
      <text class="sc-icon">⭐</text>
      <text class="sc-num">{{favCount}}</text>
      <text class="sc-label">收藏商品</text>
    </view>
    <view class="stat-card" bindtap="goHistory">
      <text class="sc-icon">👣</text>
      <text class="sc-num">{{historyCount}}</text>
      <text class="sc-label">足迹</text>
    </view>
  </view>

  <!-- ========== 我的订单 ========== -->
  <view class="section">
    <view class="sec-header">
      <text class="sec-title">我的订单</text>
      <text class="sec-more" bindtap="goOrders">查看全部 ›</text>
    </view>
    <view class="order-row">
      <view class="oi" bindtap="goOrders" data-status="unpaid">
        <text class="oi-icon">💰</text><text class="oi-label">待付款</text>
      </view>
      <view class="oi" bindtap="goOrders" data-status="toship">
        <text class="oi-icon">🚚</text><text class="oi-label">待发货</text>
      </view>
      <view class="oi" bindtap="goOrders" data-status="toreceive">
        <text class="oi-icon">📦</text><text class="oi-label">待收货</text>
      </view>
      <view class="oi" bindtap="goOrders" data-status="aftersale">
        <text class="oi-icon">↩️</text><text class="oi-label">售后进度</text>
      </view>
    </view>
  </view>

  <!-- ========== 活动横幅 ========== -->
  <view class="promo-banner" bindtap="goPromo">
    <text class="pb-tag">HOT</text>
    <text class="pb-title">周末·爆款返场</text>
    <text class="pb-btn">历史热销好款 ›</text>
  </view>

  <!-- ========== 我的资产 ========== -->
  <view class="section">
    <text class="sec-title">我的资产</text>
    <view class="asset-row">
      <view class="asset-item" bindtap="goSettings">
        <text class="asset-num">{{walletBalance}}</text><text class="asset-label">钱包</text>
      </view>
      <view class="asset-divider"></view>
      <view class="asset-item" bindtap="goCoupons">
        <text class="asset-num">{{couponCount}}</text><text class="asset-label">卡券</text>
      </view>
      <view class="asset-divider"></view>
      <view class="asset-item" bindtap="goRedPackets">
        <text class="asset-num">{{redPackCount}}</text><text class="asset-label">红包</text>
      </view>
    </view>
  </view>

  <!-- ========== 我的活动（2x3网格）========== -->
  <view class="section">
    <text class="sec-title">我的活动</text>
    <view class="activity-grid">
      <view class="ac-item" bindtap="goNewCustomer">
        <text class="ac-title">新客首单权益</text>
        <text class="ac-desc">首单4重礼</text>
        <image class="ac-img" src="/assets/icon-gift.png" mode="aspectFit"/>
      </view>
      <view class="ac-item" bindtap="goBuyer">
        <text class="ac-title">发现宝藏档口</text>
        <text class="ac-desc">2026全国精选...</text>
        <image class="ac-img" src="/assets/icon-store.png" mode="aspectFit"/>
      </view>
      <view class="ac-item ac-highlight" bindtap="goGroupBuy">
        <text class="ac-title">店主福利社群</text>
        <text class="ac-desc">运费券/红包活动</text>
        <image class="ac-img" src="/assets/icon-group.png" mode="aspectFit"/>
      </view>
      <view class="ac-item ac-hot" bindtap="goLuckDraw">
        <text class="ac-title">集财运</text>
        <text class="ac-desc">赚财运享福利</text>
        <image class="ac-img" src="/assets/icon-luck.png" mode="aspectFit"/>
      </view>
      <view class="ac-item" bindtap="goInvite">
        <text class="ac-title">邀请有奖</text>
        <text class="ac-desc">返8-100元券包</text>
        <view class="ac-badge-new">New</view>
        <image class="ac-img" src="/assets/icon-invite.png" mode="aspectFit"/>
      </view>
    </view>
  </view>

  <!-- ========== 认证店主入口（未认证时显示）========== -->
  <view class="section" wx:if="{{!isCertified}}">
    <view class="cert-entry" bindtap="goCertify">
      <text class="ce-icon">🛡️</text>
      <view class="ce-body">
        <text class="ce-title">认证店主</text>
        <text class="ce-desc">免费通过答题，解锁批发价查看权</text>
      </view>
      <text class="ce-btn">免费认证 →</text>
    </view>
  </view>

  <!-- ========== 我的服务 ========== -->
  <view class="section">
    <text class="sec-title">我的服务</text>
    <view class="service-list">
      <view class="sv-item" bindtap="goImport">
        <text class="sv-icon-text">📦</text>
        <text class="sv-label">一键导入</text>
      </view>
      <view class="sv-item" bindtap="goOneKeyImport">
        <text class="sv-icon-text">📥</text>
        <text class="sv-label">一键入库</text>
      </view>
    </view>
  </view>

  <view class="sbtm"></view>
</view>
