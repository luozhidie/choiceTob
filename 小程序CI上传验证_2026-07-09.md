# 小程序 CI 上传验证记录

**日期**:2026-07-09
**操作人**:WorkBuddy(代骆芷蝶)

## 本次提交内容(最新)
- 提交哈希:`dac06269`(基于 `aa9ed073`)
- 改动文件:
  - `miniprogram-native/pages/home/index.wxml` (菜单按钮从 header-top 移到搜索框上方)
  - `miniprogram-native/pages/home/index.wxss` (恢复轮播图高度、收紧头部间距、菜单按钮绝对定位)

## 修复点说明

### 1. 小程序标题与说明
- 标题改为「色彩风格判断问卷」
- 副标题:「商城拿货店铺上传核心VIP客户资料」
- 新增说明卡,区分:
  - 本问卷为色彩风格判断题(免费,商城拿货店铺为VIP客户填写)
  - 付费「风格测试会员」是电脑端同款14题自动诊断,做完直接出结果
  - VIP客户备注店名,视频课学员按第4题备注

### 2. 风格测试会员卡片下方新增标题
在「风格测试会员 ¥99」卡片与表单之间新增:
- 主标题:色彩风格判断问卷
- 副标题:商城拿货会员店铺核心VIP客户色彩季型风格在线诊断资料上传

### 3. 小程序 Q12/Q18 图片不显示
- 根因:`miniprogram-native/images/form-reference/` 目录不存在,图片只在 `public/images/form-reference/` 下
- 修复:将 `q12.jpg` / `q18.jpg` 复制到小程序目录,上传后小程序即可显示

### 4. 第4题文案统一
电脑版与小程序均改为:
> 4. 是否商城拿货店铺VIP客户/视频课学员，VIP客户备注店名，视频课学员备注在几号社群，微信名?(优先判断结论)

### 5. 首页粘性头部重构(本次提交)
- 需求:菜单、小标题、大标题、副标题、搜索框、分类标签全部不叠加在轮播图上,且首页上拉时文字与搜索框保持不动(粘性固定)。
- 根因:旧布局把文字层 `.hero-top/.hero-ol/.hero-mid` 绝对定位叠加在轮播 `.hero` 之上,滚动时随页面一起上移。
- 修复:
  - `index.wxml`:拆为 `.sticky-header`(含 header-top 小标题+☰菜单 / header-titles 大标题+副标题 / header-search 搜索 / header-cat-scroll 分类标签)+ 独立的 `.hero-banner` 轮播图(在头部下方,不再叠加)。
  - `index.wxss`:`.sticky-header{position:sticky;top:0;z-index:100;background:#fff;...}` 实现滚动固定;删除旧 hero 叠加样式;动态模块样式(.blk-fb ~ .pt-sub)原样保留。
  - 文字按需求落地:小标题「数据趋动·智选未来」、大标题「骆芷蝶·智选|供应链平台」、副标题「服装门店一站式赋能平台」。
  - 分类标签沿用 API `/api/public/home-categories` 返回(默认 全部/穿搭/护肤/彩妆/养生/食品/家居/文创/艺术)。

### 6. 首页布局微调(本次提交)
- 需求反馈:大轮播图被改小;头部行距偏大;菜单按钮在最顶部,希望下移到搜索框上方。
- 修复:
  - 轮播图高度从 `420rpx` 恢复为原来的 `calc(100vh - 120rpx)`(视口高度减 120rpx)。
  - 收紧头部间距:
    - `.sticky-header` 底部内边距 `20rpx → 14rpx`
    - `.header-top` 下边距 `18rpx → 8rpx`
    - `.header-titles` 下边距 `18rpx → 10rpx`, 副标题上边距 `8rpx → 6rpx`
    - `.header-search` 下边距 `18rpx → 10rpx`
    - `.tg-row` 垂直内边距 `4rpx 0 → 0`
  - 菜单按钮从 `.header-top` 中移出,改为绝对定位在 `.sticky-header` 内,位于搜索框上方偏右:
    - `position:absolute; top:calc(max(12rpx, env(safe-area-inset-top)) + 90rpx); right:28rpx; z-index:101`

## CI 上传结果
- 工作流:`.github/workflows/upload-weapp.yml`
- 运行 ID:`29032032081`
- 结果:`completed success`
  - Install miniprogram-ci → success
  - Prepare dist from native code → success
  - Upload to WeChat → success

## 结论
小程序已重新上传。首页已改为「粘性头部 + 独立轮播图」:菜单/标题/搜索/分类固定在顶部,上拉时不随页面上移;本轮进一步恢复轮播图原尺寸、收紧头部行距,并将菜单按钮下移至搜索框上方。风格测试会员卡片下方两行标题、Q12/Q18 参考图、第4题文案此前均已修复。

## 数据库备注
此前已执行的 `ALTER TABLE style_diagnoses ADD COLUMN ... photo_note/gender` 保持有效,无需再次执行。
