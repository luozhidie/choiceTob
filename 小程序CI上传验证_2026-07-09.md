# 小程序 CI 上传验证记录

**日期**:2026-07-09
**操作人**:WorkBuddy(代骆芷蝶)

## 本次提交内容(最新)
- 提交哈希:`c760bea0`(基于 `e82078ac`)
- 7 个文件变更,+194 / −87 行
- 改动文件:
  - `src/app/api/style-test/submit/route.ts` (修复 user 未定义;支持 web cookie 与小程序 user_openid;service-role 绕过 RLS;映射 wechat_id→wechat_qr_url;补充 photo_note/gender)
  - `src/app/style-test/page.tsx` (去掉底部重复的男女入口卡片,保留顶部会员卡片)
  - `miniprogram-native/pages/style-test/index.js` (新增男女切换+修复 photo_urls_1 的 f.fimg1 笔误)
  - `miniprogram-native/pages/style-test/index.wxml` (顶部会员卡片加女士/男士切换按钮)
  - `miniprogram-native/pages/style-test/index.wxss` (题目与选项间距加大,不再拥挤)
  - `supabase/migrations/20260709_style_diagnoses_add_photo_note_gender.sql` (新增 photo_note/gender 列)
  - `小程序CI上传验证_2026-07-09.md`

## CI 上传结果
- 工作流:`.github/workflows/upload-weapp.yml`(push `main` 触发 `miniprogram-native/**`)
- 运行 ID:`29011593477`
- 结果:`completed success`
  - Install miniprogram-ci → success
  - Prepare dist from native code → success
  - Upload to WeChat → success

## 需要你手动执行的一步
`style_diagnoses` 表新增了 `photo_note` 和 `gender` 列。请在 Supabase SQL Editor 执行以下 SQL:

```sql
ALTER TABLE style_diagnoses
  ADD COLUMN IF NOT EXISTS photo_note TEXT,
  ADD COLUMN IF NOT EXISTS gender TEXT;
```

或执行仓库中的迁移文件:
`supabase/migrations/20260709_style_diagnoses_add_photo_note_gender.sql`

## 结论
- 小程序代码已重新上传至微信后台,运行 `29011593477` 成功。
- Web 端三页及 API 改动随 `main` 推送由 Vercel 自动部署。
- style-test 提交接口现在能正确识别 web cookie 登录用户,以及通过小程序 `user_openid` 查找/创建用户。
